import {createMuiTheme, PaletteColorOptions} from "@material-ui/core";

export const theme  = createMuiTheme({
    palette: {
        primary: { main:'#F7B602FF'},
        secondary: { main:'#ff4081'},
    }
});